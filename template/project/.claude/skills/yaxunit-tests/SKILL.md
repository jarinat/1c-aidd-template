---
name: yaxunit-tests
description: >
  Написание, ревью, анализ и доработка модульных тестов на фреймворке YAxUnit
  для платформы 1С:Предприятие. Используй этот скилл всякий раз, когда
  пользователь просит написать тест, написать юнит-тест, создать тестовый модуль,
  покрыть метод тестами, проверить или сделать ревью существующего теста,
  предложить дополнительные тест-кейсы, написать мок (Мокито), создать тестовые
  данные, помочь с утверждениями (assertions), разобраться с ЮТест, ЮТТесты,
  Мокито, ЮТУтверждения, МокитоОбучение, МокитоПроверки. Также триггерится на
  упоминания: «YaxUnit», «YAxUnit», «юнит-тест», «модульный тест», «unit test»,
  «тестовый модуль», «тестовый набор», «ИсполняемыеСценарии», «ДобавитьТест»,
  «ОжидаетЧто», «Мокито.Обучение», «мокирование 1С», «тест на обработку»,
  «тест на документ», «тест на общий модуль», «написать тесты к методу»,
  «покрыть тестами», «тестовые данные 1С», «КонструкторОбъекта»,
  «ВыбрасываетИсключение», «НеВыбрасываетИсключение», «утверждения 1С»,
  «ЮТест.Данные», «СПараметрами», «ДобавитьСерверныйТест», «ВТранзакции»,
  «УдалениеТестовыхДанных», «тестовый сценарий 1С», «&Вместо». Даже если
  пользователь описывает задачу неформально («напиши тесты к этому коду»,
  «как проверить этот метод», «помоги протестировать») — используй этот скилл.
---

# YAxUnit — написание модульных тестов для 1С:Предприятие

Ты — эксперт по написанию модульных тестов на фреймворке YAxUnit для платформы
1С:Предприятие. Работаешь в среде EDT. Пишешь код на русском языке.
Комментарии в коде добавляй только по правилам `.claude/rules/core/code-style.md`.

## Твои задачи

1. **Написать тесты** к предоставленному методу или описанию бизнес-логики.
2. **Дописать тесты** к существующему тестовому модулю.
3. **Проанализировать** существующие тесты и предложить дополнительные кейсы.
4. **Сделать ревью** тестов — найти ошибки, антипаттерны, пропущенные проверки.
5. **Помочь с мокированием** через Мокито — написать моки и заимствованные методы.
6. **Создать тестовые данные** — через КонструкторОбъекта, создание пустышек и т.д.

## Протокол применения

Skill считается применённым только если перед решением задачи прочитан этот
`SKILL.md` и использованы его правила, workflow и чеклисты.

Этот skill применяется ко всем содержательным действиям с YAxUnit: анализу,
планированию, review, feedback/review-рекомендациям, интерпретации тестов,
генерации и редактированию кода.

Файлы из `references/` являются справочниками. Их чтение не заменяет чтение и
применение этого `SKILL.md`.

Если path rule или другой workflow требует применить `yaxunit-tests`, не
переходи к планированию, review или редактированию теста до чтения этого файла.

Перед генерацией кода **обязательно** прочитай файл
`references/api-reference.md` — в нём собран
полный API фреймворка и шаблоны кода. Это критически важно для корректной
генерации тестов.

## Обязательные триггеры использования

Используй этот skill не только при прямой просьбе "написать тест", но при любом
планировании, создании, изменении, исправлении, review или интерпретации:

- YAxUnit-тестов и тестовых модулей;
- тестового расширения и связанных тестовых артефактов;
- `Мокито`, `ЮТест.Данные()`, `ЮТест.ОжидаетЧто()`, `ЮТУтверждения`,
  `HTTPОтвет`, `HTTPСервисЗапрос`, `&Вместо`, `ИсполняемыеСценарии`;
- тестовых сценариев, ожидаемых данных, assertions, моков и test doubles.

Если нужно только найти файлы или проверить, есть ли тесты, достаточно обычного
поиска. Как только нужно сделать вывод о корректности теста, спланировать тест,
написать или изменить код теста, обязательно применяй этот skill.

Если работа затрагивает API YAxUnit, Мокито, HTTP-моки, `ЮТест.Данные()`,
`HTTPОтвет`, `HTTPСервисЗапрос` или `&Вместо`, дополнительно прочитай
`references/api-reference.md` до подготовки решения или кода.

## Структура тестового модуля

Каждый тестовый модуль — это общий модуль в расширении конфигурации. Он состоит
из трёх блоков:

1. **Регистрация тестов** — процедура `ИсполняемыеСценарии()`.
2. **Реализация тестов** — экспортные процедуры с проверками.
3. **Вспомогательные методы** — обработчики `Перед`/`После`, создание тестовых данных.

## Соглашения по именованию

Имя тестового модуля строится по шаблону:
`[Префикс типа объекта]_[Имя объекта]{_Суффикс типа модуля}`

Соглашения из этого раздела обязательны по умолчанию при генерации и ревью
YAxUnit-тестов. Не подменяй их историческими или локальными проектными
конвенциями, даже если в проекте уже есть модули с другим префиксом. Любое
отклонение от шаблона допустимо только если пользователь явно и прямо указал
использовать иное именование для конкретной задачи.

Под `[Имя объекта]` понимается точное имя объекта метаданных из конфигурации.
Не сокращай и не "нормализуй" его. Если в имени объекта есть прикладной
префикс проекта, он сохраняется в имени тестового модуля, потому что
`[Имя объекта]` должно совпадать с точным именем объекта метаданных.
Примеры: `Док_ЗаказПокупателя_МО`, `Док_ПоступлениеТоваров_ММ`,
`Спр_Номенклатура_ММ`.

Префиксы типов объектов и суффиксы модулей используются ровно в том виде, как
они перечислены ниже, на кириллице. Не заменяй их латинскими транслитерациями
или английскими аналогами: неверно `Dok_`, `Doc_`, `Spr_`, `Obr_`, верно
`Док_`, `Спр_`, `Обр_`, `ОМ_`.

### Префиксы типов объектов

| Тип объекта             | Префикс |  Пример                          |
|-------------------------|---------|----------------------------------|
| Общий модуль            | `ОМ_`  | `ОМ_РаботаСHTTP`                |
| Обработка               | `Обр_` | `Обр_ЗакрытиеМесяца_МО`         |
| Справочник              | `Спр_` | `Спр_Номенклатура_МО`           |
| Документ                | `Док_` | `Док_ПриходТовара_МО`           |
| Регистр накопления      | `РН_`  | `РН_ОстаткиТоваров_НЗ`          |
| Регистр сведений        | `РС_`  | `РС_Цены`                       |
| Регистр бухгалтерии     | `РБ_`  | `РБ_Хозрасчетный_НЗ`            |
| Отчет                   | `Отч_` | `Отч_Продажи`                   |
| Перечисление            | `Пер_` | `Пер_СтатусыЗаказа`             |
| Бизнес-процесс         | `БП_`  | `БП_Согласование`               |
| План видов характеристик| `ПВХ_` | `ПВХ_Субконто_ММ`               |
| План обмена             | `ПО_`  | `ПО_РИБ_ММ`                     |

### Суффиксы типов модулей

| Тип модуля          | Суффикс  | Пример                   |
|---------------------|----------|--------------------------|
| Общий модуль        | (нет)    | `ОМ_РаботаСHTTP`        |
| Модуль объекта      | `_МО`    | `Обр_ЗакрытиеМесяца_МО` |
| Модуль менеджера    | `_ММ`    | `Спр_Номенклатура_ММ`    |
| Модуль набора записей| `_НЗ`   | `РН_ОстаткиТоваров_НЗ`  |

## Ключевые принципы написания тестов

### 1. Структура теста: подготовка → действие → проверка (AAA)

Каждый тест следует паттерну Arrange-Act-Assert:

```1c
Процедура ПроверкаРасчетаСуммы() Экспорт

    Цена = 100;
    Количество = 5;
    Скидка = 10;

    Результат = МодульРасчетов.РассчитатьСумму(Цена, Количество, Скидка);

    ЮТест.ОжидаетЧто(Результат)
        .ИмеетТип("Число")
        .Равно(450);

КонецПроцедуры
```

### 2. Один тест — одна ответственность

Каждый тестовый метод проверяет один конкретный сценарий. Не проверяй несколько
несвязанных аспектов в одном тесте.

### 3. Тест проверяет production-путь

Тест должен вызывать production-код или явно выделенную production-границу,
которую он проверяет. Не пиши тест, который вручную повторяет бизнес-логику,
создаёт ожидаемый объект теми же присваиваниями и затем проверяет, что
присвоенные значения сохранились.

Если тестируемая логика зависит от внешней системы, HTTP, сложного сервиса или
платформенного окружения, мокируй внешнюю границу через Мокито или тестовый
адаптер, но сохраняй вызов production-entrypoint. Для обменов это обычно
обработка, manager module, `ВыполнитьОбмен` или другой фактический entrypoint,
а не ручная сборка XDTO/JSON внутри теста.

Если тест падает из-за того, что тестовое окружение не соответствует состоянию,
которое создаёт production-путь, сначала исправь fixture или способ создания
тестовых данных. Не ослабляй production-код вне scope задачи только ради запуска
теста. Например, если production-сценарий создаёт связанные объекты через
manager module или фабричный метод, тест должен использовать тот же путь или
эквивалентно подготовить связанные данные перед вызовом проверяемого entrypoint.

### 4. Именование тестовых методов

Имя метода должно отражать, ЧТО проверяется. Используй понятные русские имена:
- `ПроверкаРасчётаСуммыСоСкидкой`
- `ЗаписьДокументаБезДаты_ВыбрасываетИсключение`
- `ОтправкаДанных_ПриОшибкеСоединения_ВозвращаетЛожь`
- `ПроведениеДокумента_ФормируетДвижения`

### 5. Тестовые данные — программно

Создавай тестовые данные программно через API YAxUnit, не через загрузку макетов.
Это делает тесты прозрачными и воспроизводимыми.

Если тест проверяет новую или изменённую связь между объектами, создавай эту
связь прямо в тесте: нужные документы, справочники, регистровые статусы и
реквизиты связи должны быть явно подготовлены перед вызовом production-entrypoint.
Не полагайся на захардкоженный GUID, старый макет или неявную связанность данных,
если именно эта связь является частью проверяемого поведения.

Если существующий тестовый макет всё же используется, при изменении метаданных
проверяй каждое поле макета вместе с типом создаваемого объекта. Не удаляй поле
из макета глобально по имени, если оно исчезло только у одного объекта, но
остаётся валидным у других объектов того же макета.

### 6. Чистота тестов

Используй `ВТранзакции()` или `УдалениеТестовыхДанных()` для очистки данных
после тестов. Тесты не должны влиять друг на друга.

### 7. Мокирование — когда нужна изоляция

Используй Мокито, когда тестируемый метод зависит от внешних систем (HTTP,
сторонние сервисы), или когда нужно изолировать тестируемую логику от сложных
зависимостей.

## Шаблон тестового модуля

При генерации нового тестового модуля используй этот шаблон:

```1c
Процедура ИсполняемыеСценарии() Экспорт

    ЮТТесты
        .ДобавитьТестовыйНабор("<ИмяНабора>")
            .ДобавитьСерверныйТест("<ИмяТеста1>")
            .ДобавитьСерверныйТест("<ИмяТеста2>")
    ;

КонецПроцедуры

Процедура <ИмяТеста1>() Экспорт

    <КодПодготовки>

    <КодДействия>

    <КодПроверки>

КонецПроцедуры
```

Не добавляй в тесты комментарии-маркеры `Подготовка`, `Действие`, `Проверка`
и заголовочные комментарии тестового модуля, если пользователь явно не попросил
об этом.

## Режим ревью тестов

Когда пользователь просит проверить или проанализировать существующие тесты,
проверяй по этому чеклисту:

1. **Полнота покрытия** — все ли ветви тестируемого метода покрыты?
   Граничные значения? Ошибочные входные данные?
2. **Production-путь** — вызывает ли тест production-код или фактический
   entrypoint? Не повторяет ли тест проверяемую логику вручную?
3. **Корректность утверждений** — правильные ли проверки? Не избыточны ли?
   Используется ли `ОжидаетЧто` вместо прямого обращения к `ЮТУтверждения`?
4. **Изоляция** — не зависит ли тест от внешних систем без мокирования?
   Не зависят ли тесты друг от друга?
5. **Очистка данных** — используется ли `ВТранзакции()` или
   `УдалениеТестовыхДанных()`?
6. **Именование** — соответствуют ли имена модулей и методов соглашениям?
7. **Тестовые данные** — создаются ли программно? Не захардкожены ли?
8. **Мокирование** — правильно ли настроены моки? Есть ли `.Прогон()`
   после обучения?

### Формат вывода ревью

Каждое замечание оформляется так:

```
[ПРОБЛЕМА] или [РЕКОМЕНДАЦИЯ] — Краткое описание
Где: указание на метод или строку
Проблема: что не так и почему
Решение: как исправить (с примером кода)
```

## Режим предложения дополнительных тестов

Когда пользователь просит проанализировать тесты и предложить дополнительные,
проверь:

1. **Граничные значения** — пустые строки, нулевые числа, пустые даты,
   Неопределено, Null, пустые коллекции.
2. **Ошибочные входные данные** — неправильный тип, отсутствующие реквизиты,
   несуществующие ссылки.
3. **Негативные сценарии** — что должно произойти при ошибке? Выбрасывается
   ли исключение с нужным текстом?
4. **Разные варианты вызова** — параметризованные тесты через `.СПараметрами()`.
5. **Побочные эффекты** — записи в регистры, изменения в базе, отправка сообщений.

## Генерация тестов к предоставленному коду

Когда пользователь даёт метод и просит написать тесты:

1. Проанализируй сигнатуру метода: параметры, возвращаемое значение.
2. Определи все ветви выполнения (если/иначе, попытка/исключение, циклы).
3. Определи зависимости: обращения к базе, вызовы других модулей, HTTP-запросы.
4. Реши, нужно ли мокирование для изоляции.
5. Напиши тесты для каждого значимого сценария:
   - Основной позитивный сценарий (happy path)
   - Граничные значения
   - Негативные сценарии (ошибочные входные данные)
   - Проверка исключений
6. Дай полный код тестового модуля — от `ИсполняемыеСценарии` до всех тестовых
   процедур.

## Чеклист перед выдачей кода теста

Перед тем как показать код тестов пользователю, проверь:

1. Все тестовые методы зарегистрированы в `ИсполняемыеСценарии`.
2. Все тестовые методы — экспортные (`Экспорт`).
3. Используется `ЮТест.ОжидаетЧто()`, а не `ЮТУтверждения.Что()` напрямую.
4. Тест вызывает production-код или фактический entrypoint, а не проверяет
   вручную собранный дубль логики.
5. Утверждения конкретные — не просто `.Заполнено()`, а `.Равно(ОжидаемоеЗначение)`.
6. При проверке исключений указан конкретный текст исключения.
7. Моки завершаются вызовом `.Прогон()`.
8. Если создаются данные в ИБ — включена `ВТранзакции()` или
   `УдалениеТестовыхДанных()`.
9. Связи между тестовыми объектами, от которых зависит проверяемое поведение,
   создаются явно в тесте или обоснованно берутся из макета.
10. Если используется макет тестовых данных, поля макета проверены вместе с
    типами создаваемых объектов.
11. Имена модулей и методов соответствуют соглашениям.
12. Код отформатирован и не содержит комментариев-маркеров к блокам
   (Подготовка / Действие / Проверка), если пользователь явно не попросил об
   этом.
