# Ревизия agent layer Hermes (итерация B)

Дата: 2026-08-08
Спека: ../specs/2026-08-08-hermes-agent-layer-review-design.md
План: ../plans/2026-08-08-hermes-agent-layer-review.md

Объект: репозиторий `hermy-PROSYS-Avtolid`, ветка `experiment/edtpt-agent-layer`,
HEAD: `4384d6d22c865b058e5a3091361d4c5a68be9396`
(2026-08-07, «EDTPT-AI: harden renderer check and cross-platform tests»).
Файлы читаются из worktree
`C:/ai-agents/hermes/workspace/git/sollers/hermy-PROSYS-Avtolid-agent-layer` (W).

Формат вердиктов: принять / доработать / выбросить; решение: engine / user.

## 0. Ограничения и фиксация объекта

- Оба worktree чистые (`status -sb` без изменений): основной — на
  `feature/MPLSYS-4793-edtpt-agent-layer` (HEAD `9f57043a5`, merge от
  2026-08-07), экспериментальный — на `experiment/edtpt-agent-layer`
  (`4384d6d22`).
- Ветка `feature/MPLSYS-4793-edtpt-agent-layer` ОТСТАЁТ от эксперимента на
  последний коммит `4384d6d22`: он содержится только в
  `experiment/edtpt-agent-layer`. Объект ревизии — вершина эксперимента
  (полная итерация B).
- Non-merge коммитов `EDTPT-AI:` — 12 (не 13, как предварительно оценивалось):
  `28ffe87ac, 3e968f43a, 44b736d95, 69351c62f, f966da8df, fa5978d02,
  49452739d, 48e9ccc4a, 189ac8a7a, b2458fcc0, 1e7a7c66b, 4384d6d22`.
  Смежный коммит пользователя: `3ba5e293d` «MPLSYS-4793: каталог .claude
  удален из .gitignore» — не Hermes, но входит в механику слоя (selective
  tracking) и учитывается в задачах 2 и 7.
- Затронутые зоны: `AGENTS.md`, `.agents/**`, `.claude/**`, `doc/ai/**`
  (изначально `docs/ai/**`, переименовано в `49452739d`), `tools/ai/**`,
  `.gitignore`, `.gitattributes`, корневой `CLAUDE.md` (создан в `69351c62f`,
  перемещён в `.claude/CLAUDE.md` в `fa5978d02`). Файлов вне ожидаемых зон
  не обнаружено; `.gitignore`/`.gitattributes` — инфраструктурные правки,
  детально проверяются в задачах 2 и 7.
- Состав слоя на вершине: 14 канонических skills `.agents/skills/mplsys-*`
  (+2 references), роль `.agents/roles/mplsys-implementer/ROLE.md`,
  14 адаптеров `.claude/skills/mplsys-*`, `.claude/agents/mplsys-implementer.md`,
  `.claude/CLAUDE.md`, `.claude/rules/mplsys/mplsys.md`, `doc/ai/README.md`,
  `doc/ai/project/{project-rules.template.md,project-rules.example.md}`,
  9 стандартов `doc/ai/standards/*`, `tools/ai/{mplsys-commit.sh,
  mplsys-render-claude-layer.sh,mplsys-validate-agent-layer.py}`,
  `tools/ai/tests/{run-selftests.sh,fixtures/sonar-issues.sample.json}`.

## 1. Испытания и отчёт Hermes

Найденные артефакты: каталог `.ai-eval/` (40 файлов, ignored) в worktree W.
Итоговый отчёт Hermes (15 пунктов по промпту) и матрица испытаний **как файлы
отсутствуют** — по-видимому, остались только в сессии Hermes.

Что фактически запускалось (по логам `.ai-eval/`):

| Испытание | Evidence | Статус |
|---|---|---|
| Discovery Codex (clean) | `codex-clean-discovery.txt`: Codex v0.144.5 видит слой, называет skills и commit-правило | подтверждено |
| Discovery Claude (clean) | `claude-clean-discovery.txt`: мост загружен, но на раннем этапе Claude НЕ мог перечислить skills из startup context (только `mplsys-code-review`) | подтверждено, с находкой |
| Discovery после переносов (`.claude`, `doc/ai`, новые skills) | `*-final.txt`, `claude-three-skills-startup-final.txt`, `codex-after-dotclaude-final.txt` | подтверждено |
| Сосуществование с личным слоем | `claude-dotclaude-local-coexistence.txt` (маркер из `CLAUDE.local.md` + корпоративные инварианты), `codex-personal-layer.txt` | подтверждено |
| Защита namespace | `namespace-conflict.txt`: validator (227 проверок) ловит подсадной `.claude/skills/mplsys-conflict-probe` | подтверждено |
| Commit helper | `commit1..4-dryrun.txt`: реальные dry-run прогоны | подтверждено |
| Языковая политика | `*-russian-policy-final.txt` | подтверждено |
| «Natural/explicit suites» обоих CLI (9 кейсов A–I) | `claude-natural-suite-full.md`, `codex-natural-suite.txt` и пр.: **routing-only** — «no actions were executed», PASS означает выбор правильного skill и правильных гейтов, а не исполнение сценария | подтверждено как routing, НЕ подтверждено как исполнение |
| Перекрёстное ревью hardening | `hardening-review.txt`, `hardening-final-review.txt`: содержательное критическое ревью (см. находки ниже) | подтверждено |

Известные проблемы, зафиксированные самим экспериментом и не имеющие
подтверждённого закрытия в логах:

- `str | None` в `mplsys-validate-agent-layer.py` поднимает минимальный Python
  до 3.10 без явной фиксации требования (hardening-final-review, пункт 1) —
  проверить фактическое состояние кода в задаче 6.
- Positive self-test `c-dynamic-skill` корректен только при динамическом
  обнаружении skills рендерером — требует подтверждения кода (задачи 5–6).
- Suites A–I запускались ДО переименования `docs/ai` → `doc/ai`; после
  финальных переносов повторялись только startup-discovery проверки, полный
  прогон suites не повторён.

Проверка «непересекаемых границ» промпта:

- `EDT-project-template` main не тронут: вершина `a9e5555` (2026-08-04, до
  старта Hermes). Ветка `feature/team-ai-tools` (итерация A) создана 31.07 —
  до эксперимента, Hermes ей не принадлежит.
- `1c-aidd-template` (донор) не тронут: история чистая.
- Push не выполнялся: `experiment/edtpt-agent-layer` и
  `feature/MPLSYS-4793-edtpt-agent-layer` существуют только локально.

## 2. Entrypoints

| Артефакт | Находки | Вердикт | Решение |
|---|---|---|---|
| `AGENTS.md` | Роутер (таблица «задача → skill»), всегда загружаемые инварианты безопасности, каталоги skills/ролей, личное расширение, команды валидации. Дублирования процедур нет — только указатели. Осознанно длиннее «голого указателя», это задокументировано в `doc/ai/README.md` и охраняется валидатором (routing-таблица и commit-safety текст проверяются кодом). Минус: утверждение про семантику `AGENTS.override.md` (полная замена) не сверено с официальной документацией Codex — на нём построено предупреждение о shadowing | принять; сверить факт про `AGENTS.override.md` при переносе | engine |
| `.claude/CLAUDE.md` (мост) | Первая директива — активный импорт `@../AGENTS.md`; соответствует официальной механике Claude Code (относительный путь от файла-владельца; путь внутри repo ⇒ external-import диалога не будет). Мост тонкий: 3 Claude-инварианта, без копий стандартов. Windows-совместимо (не симлинк) | принять | engine |
| Сосуществование с личным слоем разработчика | **Главный gap**: у разработчика с личной обвязкой (как MBS: untracked `AGENTS.md` и `.claude/CLAUDE.md` с AIDD) `git merge` ветки шаблона откажется перезаписывать untracked-файлы тех же путей — установка слоя требует предварительной миграции личных файлов (в `CLAUDE.local.md` / `~/.codex/AGENTS.md` / `.agents/local/`). В итерации A такая миграционная инструкция была (doc/README.md A); в B — отсутствует. Дополнительно: личные ignored `.claude/rules/*` продолжат нативно грузиться Claude вместе с корпоративными `.claude/rules/mplsys/` — композиция AIDD+корпоративного слоя в одном контексте возможна, но не описана | доработать: добавить в `doc/ai/README.md` раздел «Обновление существующего проекта с личным слоем» | user (подтвердить состав инструкции) |
| `doc/ai/README.md` | Полное и честное описание архитектуры: три слоя, discovery обоих CLI, selective tracking, контракт валидатора, расширение слоя. Модель поставки «cherry-pick в шаблон → merge в проекты» совпадает с принятым решением (фиче-ветка шаблона, pull в MBS) | принять | engine |
| `doc/ai/standards/layers.md` | Чёткое правило размещения («не можешь сформулировать без имени проекта — не корпоративное»), защита namespace, приоритет слоёв, поведение при конфликте (вынести человеку). | принять | engine |
| `doc/ai/project/project-rules.template.md` + `example` | Четыре корзины (факты/допущения/вопросы/решения) с обязательным источником у факта — совпадает по духу с onboarding-философией донора. Example помечен как вымышленный | принять | engine |
| `.gitignore` (selective tracking) | Фактический файл соответствует документации: `.claude/*` игнорируется, разыгнорированы только `CLAUDE.md`, `rules/mplsys/`, `skills/mplsys-*/`, `agents/mplsys-*.md`; дополнительно игнорируются `/CLAUDE.local.md`, `.agents/local/`, `.mcp.json`, `.ai-eval/` | принять | engine |
| Языковой стиль | Русский выдержан, маркеров AIDD/Avtolid нет (проверено grep). Минус вкусовой: обилие несклоняемых англицизмов («layer'а», «adapter'ы») рядом с русскими терминами | принять; при переносе можно причесать | user (вкус) |

## 3. Standards

Сверка с донором: `onec-general.md`, `code-style.md`, `code-review.md`,
`git-workflow.md`, `tool-usage.md` (текст в контексте ревизии идентичен
MBS-копиям донора), skills `1c-query` и `yaxunit-tests`.

| Артефакт | Находки | Вердикт | Решение |
|---|---|---|---|
| `onec-queries.md` | Высокое качество: параметры vs конкатенация, кавычки в литералах, семантика при рефакторинге, кардинальность LEFT, СоставнойТип/ВЫРАЗИТЬ без рефлекторности, NULL, виртуальные таблицы/периодичность, sargable-условия (порядок предикатов честно назван не-оптимизацией), временные таблицы, «БД в цикле» сохранён С межметодным случаем донора, реальность метаданных + validate_query. Выдумок не найдено | принять | engine |
| `testing-yaxunit.md` | Донорская философия сохранена: фактическая версия/API из исходников, запрет выдуманного API Мокито/ЮТест, unit/integration/smoke, продуктивная точка входа, ассерты на значения (не `ЗначениеЗаполнено`), изоляция/очистка, «нет PASS без запуска», безопасная ИБ | принять | engine |
| `code-review.md` | Review-ядро донора выжило: BASE vs регрессия, полный diff, факты vs вкус, БД в циклах (включая скрытые), пакетное чтение реквизитов одной ссылки, read-then-write без блокировки, привилегированный режим после проверки права, полнота ролей против sibling-объектов, запрет прямых прав на перечисления, переиспользование vs самописный helper, тесты — чтением реальных модулей, анализатор ≠ замечание без подтверждения | принять | engine |
| `git-commits.md` | Ядро донорского git-workflow + «никогда не коммить» список; формат сообщения (тикет-префикс) корректно вынесен в правило проекта — важная граница для команды | принять | engine |
| `agent-tools.md` | Дистиллят донорского tool-usage + EDT MCP паттерны: узкий инструмент, discovery-before-content, один механизм записи, оптимистичная конкурентность, точечная валидация вместо полной пересборки, двухфазные разрушительные операции, тяжёлые операции только по решению человека, честная фиксация пробелов | принять | engine |
| `language.md` | Продуманная политика: что переводится/не переводится, англицизмы легализованы и склоняются — закрывает стилевой вопрос из секции 2 | принять | engine |
| `onec-bsl.md` | **Главная потеря содержания.** Осталась generic-инженерия (клиент/сервер, транзакции, совместимость, НСтр). Потеряно переносимое write-time ядро донора: пакетное чтение реквизитов ссылки (`ЗначенияРеквизитовОбъекта`) как правило написания кода; проверки уникальности под управляемой блокировкой + парный `dataLockFields`; `ПередЗаписью`/`ПриЗаписи` и проброс контекста через `ДополнительныеСвойства`; привилегированный режим (write-time правило, не только review-чек); GUID/UUID в EDT XML; обработчики обновления ИБ; «изменение обмена = изменение контракта»; границы ответственности процедур и транзитные параметры. Часть дублируется review-чеками, но конструктивных правил на этапе written-кода нет | доработать: обогатить переносимым ядром донора | user (состав пунктов) |
| `bsl-style.md` | Generic-стиль. Два вопроса: (1) «Комментируйте почему» мягче донорского «по умолчанию без комментариев в коде; экспортные — только полноценный EDT doc-comment» — прямой конфликт философий; (2) потеряны конкретные донорские правила: формат EDT doc-comment, `СтрШаблон` для многострочных/многоподстановочных строк, `Е` вместо `Ё`, запрет смысловых сокращений в именах. Вопрос в том, какие из них команда готова принять как корпоративные, а какие — ваш личный стиль для слоя 3 | доработать | user (какие правила возвести в корпоративные) |

Чистота: маркеров AIDD/Avtolid/донора в стандартах нет (grep). Выборочная
проверка фактических утверждений (порядок предикатов `ГДЕ`, `ПОДОБНО` и
индекс, `В (&Список)` vs временная таблица) — корректны, выдуманных API не
обнаружено.

## 4. Skills

Проверены все 14 канонических `.agents/skills/mplsys-*/SKILL.md` + роль
`.agents/roles/mplsys-implementer/ROLE.md`. Frontmatter везде: `name` = имя
каталога, `description` с внятными триггерами; каждый skill самодостаточен;
ссылок на донора/AIDD/личные пути нет.

| Артефакт | Находки | Вердикт | Решение |
|---|---|---|---|
| `mplsys-edt-mcp`, `mplsys-edt-companion-mcp`, `mplsys-1c-rsv` | Discovery-first, запрет выдумывания schema, правило единственной записи, оптимистичная конкурентность, точечная валидация, двухфазность деструктивных операций. Имена tools сверены с реальными контрактами (edt-mcp/companion — по донорским скиллам, 1c-rsv — по фактическим MCP этой сессии) — выдумок нет. **Policy-расхождение с донором**: у Гермеса «другой явно настроенный EDT MCP» — допустимый документированный fallback; ваше правило — «другой EDT MCP не является fallback», ровно один сервер в `.mcp.json` | принять; fallback-политику решить | user (fallback на другой EDT MCP: разрешить или запретить) |
| `mplsys-1c-query` | Тонкая процедура над стандартом: намерение → подтверждение метаданных → приоритет корректность/кардинальность/NULL → семантика → validate. Режим review отдельно | принять | engine |
| `mplsys-yaxunit` + `references/yaxunit-api.md` | Обязательный шаг «определи фактическую версию и API из исходников»; reference оформлен как чек-лист для сверки, а не замена сверки; перечисленные модули/методы (`ЮТест`, `ЮТУтверждения`, `ЮТТесты`, `Мокито`, `ЮТКонтекстТеста`) соответствуют реальному API YAxUnit | принять | engine |
| `mplsys-1c-bsp` | Переносимый контракт без project-имен; честно фиксирует, что подтверждён только reference `report-variants.md`, остальные подсистемы — discovery по проекту | принять | engine |
| `mplsys-v8std` | Опциональность оформлена правильно: недоступность MCP не блокирует работу и не ведёт к самонастройке | принять | engine |
| `mplsys-metadata-removal-impact` | Донорская классификация `UNUSED`/`DEAD_FUNC`/`USED`/`UNCLEAR` и жёсткий gate сохранены; явно указано, что нижние слои gate не отменяют | принять | engine |
| `mplsys-code-review` | Read-only жёстко; полный diff + окружение; BASE vs регрессия; чек-лист повторяет стандарт; анализатор — только подтверждённое evidence | принять | engine |
| `mplsys-address-review` | Только выбранные замечания; повторное подтверждение применимости; минимальный fix причины; матрица «замечание → изменение → проверка»; без commit | принять | engine |
| `mplsys-fix-sonar` | Две фазы (evidence / fix) разделены; offline fixture без токена; отклонение фиксируется, не скрывается; «требует решения» выносится человеку; чужой Sonar-скрипт не захватывается | принять | engine |
| `mplsys-commit` | Только явные пути, dry-run обязателен, отказ при непустом индексе без переопределения, без push; формат сообщения — правило проекта | принять | engine |
| `mplsys-project-onboarding` | Read-only по умолчанию; четыре корзины; материализация `project-rules.md` только по явному запросу; «один пример — не правило» | принять | engine |
| `mplsys-implement` + роль `mplsys-implementer` | **Не** является протащенным AIDD: нет lifecycle, артефактов, статусов, тикетов — generic-оркестрация «нормализуй → последовательные проверяемые блоки → делегируй по одному → проверь diff и прогони проверки сам». Handoff — строгий YAML-контракт из 6 полей. Но: (1) это добавка сверх MVP промпта — фактически первый шаг к «стабильному воркфлоу», который вы планировали позже; (2) `.agents/roles/` — изобретение, не стандарт Codex: нативная проекция есть только у Claude (`.claude/agents/mplsys-implementer.md`), для Codex роль — просто текст, исполняемый по инструкции; (3) frontmatter роли (`capabilities:`) — тоже собственное соглашение слоя, охраняемое только своим валидатором | принять или отложить — решение по составу первого релиза | user (включать ли implement+роль в первый релиз слоя) |

Сквозные наблюдения:

- Непоследовательное обращение: EDT/инженерные skills на «ты», процессные — на
  «вы». Мелочь, но в корпоративном слое лучше единообразие — при переносе
  выровнять (вкус, не блокер).
- Ни один skill не требует запуска другого skill; удаление любого не ломает
  остальные (перекрёстные ссылки — только «см. также»/маршрутизация).

## 5. Мост и рендер Claude

| Артефакт | Находки | Вердикт | Решение |
|---|---|---|---|
| `tools/ai/mplsys-render-claude-layer.sh` | Качество выше ожидаемого: динамическое обнаружение skills и ролей (открытый вопрос hardening-review закрыт в коде); preflight fail-closed против инъекций через имя каталога и frontmatter (включая U+2028/U+2029/C1, CRLF-устойчивость); printf-as-data вместо heredoc (инъекционная безопасность); `--check` строго побайтовый; исправлен subshell-баг fail-open. Мост `.claude/CLAUDE.md` осознанно НЕ генерируется (рукописный, охраняется валидатором) | принять | engine |
| Адаптеры `.claude/skills/mplsys-*` (14) | Выборочно сверены (`mplsys-code-review` и др.) — побайтово соответствуют шаблону рендера: frontmatter с каноническим description + указатель, «второй копии процесса» нет. Цена: при вызове skill Claude должен дочитать канонический файл по ссылке — осознанный трейд-офф ради единого источника | принять | engine |
| `.claude/rules/mplsys/mplsys.md` | Совпадает с `render_rules` рендера; корректно описывает оба пути загрузки (мост-импорт + нативные rules/skills) | принять | engine |
| `.claude/agents/mplsys-implementer.md` | Сгенерирован по шаблону: frontmatter `permissionMode: acceptEdits` + `disallowedTools` (без allowlist `tools` — обоснование про отрезание MCP корректно для Claude Code), тело роли перенесено дословно, маркер «не редактируй вручную» | принять (если implement+роль остаются — см. секцию 4) | user (связано с вопросом о включении implement) |
| Архитектура моста в целом | Рендер+строгий `--check` против альтернатив: симлинки отвергнуты справедливо (Windows), идентичные ручные копии — хуже (дрейф). Для обычного разработчика цена нулевая: рендер нужен только при изменении корпоративного слоя. Открытый вопрос — где гарантированно запускать `--check` и validator (bash+python3 на машинах команды/раннере CI — см. секцию 6) | принять | engine |

## 6. Валидация и self-tests

## 7. Commit helper, fixtures и чистота коммитов

## 8. Сверка с итерацией A (mpl-*)

## 9. Не подтверждено испытаниями

Предварительный список по итогам задачи 1 (уточняется задачами 2–7):

1. Итоговый отчёт Hermes и матрица испытаний — файлов нет; полнота
   эксперимента восстанавливается только по `.ai-eval/` логам.
2. End-to-end исполнение сценариев промпта (реальное исправление запроса 1С,
   написание и прогон YAxUnit-теста, review синтетического diff с внесением
   правок address-review, обработка Sonar fixture с правками кода) — suites
   были routing-only; поведение skills при фактическом исполнении не
   проверялось ни на одном CLI.
3. Полный прогон suites после финальной структуры слоя (`doc/ai`,
   `.claude/CLAUDE.md`, 14 skills) — не повторялся; финальное состояние
   проверено только startup-discovery.
4. Прохождение полного `run-selftests.sh` на финальной вершине `4384d6d22` —
   лога нет (validator запускался, 227 checks, но единый «all pass» прогон
   self-tests не зафиксирован).
5. Сценарий «чистый разработчик» из чистого checkout только tracked-файлов —
   метод в логах не зафиксирован; discovery-логи есть, но воспроизводимость
   именно из чистого дерева не доказана.
6. Работоспособность слоя на Windows-машине разработчика (все helper'ы —
   bash/python; эксперимент шёл в Linux-контейнере Hermes).

## 10. Вход для дизайна самоулучшения

## 11. Сводка вердиктов и доработки для подпроекта 2
